apiVersion: v1
kind: Pod
metadata:
  name: resources
  
spec:
  containers:
    - name: nginx-container
      image: nginx:latest
      resources:
        requests: #soft limit
          memory: "68 mi"
          cpu: "100m"
        limits:
          memory: "128Mi"
          cpu: "150m"

          
